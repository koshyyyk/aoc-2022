use crate::{InputIterator, Ztr};

static TEST_DATA: &str = "";

pub fn solution(i: InputIterator) -> (Ztr, Ztr) {
    ("--".into(), "--".into())
}

#[test]
fn test1() {
    assert!(true);
}

#[test]
fn test2() {
    assert!(true);
}
